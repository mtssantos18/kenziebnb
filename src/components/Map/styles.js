import styled from "styled-components";

export const Container = styled.div`
  width: 95%;
  margin: 0 auto;
  position: relative;

  h3 {
    font-weight: 600;
    font-size: 16px;
    line-height: 19px;
    color: var(--grey-4);
    margin-bottom: 10px;
  }
`;
